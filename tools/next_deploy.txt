v121 — legacy upload history, archive index, and the settings-drift warning

# Everything above the --- is the commit message (first non-comment line).
# Everything below is extra server work for this deploy only.
#
# NOTHING extra on the server: the new setting (archive_index_chunk) and the
# new table (store_count_checks, from v120) are applied on startup.
#
# THE WORKER MACHINE NEEDS worker_service\ COPIED AGAIN — it gains
# archive_index.py and the agent learns the "archive_index" job. No new pip
# packages; it uses os.walk and nothing else.
#
# AFTER DEPLOYING, in this order:
#   1. MIGRATE.bat -> LIST ACCOUNTS -> 6b CHECK -> read the numbers -> 6c
#   2. Pipeline page -> READ THE STORAGE BOX
