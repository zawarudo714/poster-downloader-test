v122 — external_id repeats across projects; the legacy import was matching MUSIK

# Everything above the --- is the commit message (first non-comment line).
# Everything below is extra server work for this deploy only.
#
# Nothing extra on the server.
#
# The worker machine still needs worker_service\ copied if you have not done
# it since v120 — agent 1.26.0. Diagnostics will say so.
#
# AFTER DEPLOYING:
#   MIGRATE.bat -> LIST ACCOUNTS -> 6b CHECK
#   Expect roughly 4,800 recordable and ~50 unplaceable. If 6b still says
#   almost nothing can be matched, it now REFUSES to let 6c run.
