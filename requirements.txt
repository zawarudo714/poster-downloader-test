fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.9
bcrypt==4.2.0
itsdangerous==2.2.0
requests==2.32.3
SQLAlchemy==2.0.35
openpyxl==3.1.5
# tzdata bundles the IANA timezone database — required for zoneinfo.ZoneInfo
# inside Python on slim/alpine images that don't ship system tzdata.
tzdata==2024.2
# Fernet encryption for marketplace account passwords in the post-production
# pipeline (app/pipeline.py). The key derives from PIPELINE_SECRET, falling
# back to SESSION_SECRET. See PIPELINE.md §8 before rotating either.
cryptography==43.0.1
