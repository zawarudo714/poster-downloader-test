Deploy tool: an empty commit message no longer silently skips the commit and
pushes stale code — that is why versions 103 and 104 never left the laptop. It
now commits as "deploy vNNN" and says so. New local/live version label with a
CHECK LIVE button. Also fixes the Listing check tab rendering with no
navigation, and a preflight check so that cannot happen again.
