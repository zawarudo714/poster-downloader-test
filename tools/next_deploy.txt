New LISTING CHECK tab (master level, beside TeePublic): opens each FAA
listing's own address and reports whether it still loads. Read-only, HEAD
requests, no browser and no sign-in — it does NOT hold the pipeline. Manual
only, no schedule. Each account needs its artist name typed in first, and a
sweep where almost everything reads GONE stops itself rather than reporting
thousands of takedowns. Also removed two dead controls. Node must be updated
too: agent 1.24.0.
