# Not yet deployed

**v215 — on a phone, the flag card row holds only the paste box and
REPLACE FILE.** The v214 fix put all three controls on one row, which
starved the paste box of space. FIND A REPLACEMENT now takes its own full
row below the pair, as the owner asked. Server only — the node needs no
copy.

Whoever changes code writes here what is waiting and why; the deploy tool
empties this file once the server is confirmed to be running it.
