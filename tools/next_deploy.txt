The deploy tool now proves the RUNNING site matches the code you pushed, not
just that the commit arrived. /healthz reports the version, and a mismatch is
reported loudly and NOT written to the deploy log. Also fixes a real bug: any
job running longer than 45 minutes was declared abandoned even while it was
reporting every few seconds, which cancelled a healthy TeePublic switching job
and re-dispatched its last 8 designs. Node needs the folder copy too.
