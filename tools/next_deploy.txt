Deploy now runs tools/preflight.py first: compiles, undefined names, JS parsing,
template tags, page hooks, button handlers, settings keys, cross-module calls.
python tools/preflight.py --map lists every button and what it calls.
