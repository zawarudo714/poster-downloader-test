Added .dockerignore. The Dockerfile ends with COPY . . and there was no ignore
file, so every build packaged the whole data folder into the image — 8.5GB of
posters and the database. Production's image was 21.87GB for a 3MB codebase.
Also SERVERS.bat: one click to a logged-in terminal on either box, using an SSH
key installed once instead of a saved password.
