v120 — the wall now stops a reactivation instead of blaming 79 designs

# Everything above the --- is the commit message (first non-comment line).
# Everything below is extra server work for this deploy only.
#
# NOTHING extra is needed on the server: the new columns
# (store_listings.action_fail_count, upload_accounts.inactive_count /
# inactive_checked_at) and the new table store_count_checks are applied by
# app/schema_migrations.py and create_all() on startup.
#
# THE WORKER MACHINE MUST BE UPDATED TOO. Copy worker_service\ across and
# restart the agent — the switch-on wall check, the real click and the
# inactive-count reading all live there. No new pip packages.
